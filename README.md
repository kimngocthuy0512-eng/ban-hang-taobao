The product image extraction logic in `server/index.js` has been updated with improved fallback mechanisms.

Please restart your server for these changes to take effect.
You will also need to manually stage and commit the changes to `server/index.js` and `README.md`.